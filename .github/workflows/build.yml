# Inherited binary build/test/publish pipeline disabled in this fork.
# No push/tag trigger and no binary uploads until corresponding source is complete.
name: Legacy publishing disabled
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  blocked:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Publishing blocked. Use the manual native inventory workflow."
